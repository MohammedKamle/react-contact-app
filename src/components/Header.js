import React from "react";

// functional component
const Header = () => {
    return (
        // Using libraries from semantic-ui
        <div className="ui fixed menu">
            <div className="ui container center">
                <h2>Contact Manager</h2>
            </div>
        </div>
    );
};

export default Header;